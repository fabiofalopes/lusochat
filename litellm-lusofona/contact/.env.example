# Gmail Bulk Sender Configuration
# Copy this to .env and customize for your setup

# Authentication (choose one approach)
AUTH_MODE=oauth
CLIENT_SECRETS_PATH=./credentials/<client_id>.apps.googleusercontent.com.json
TOKEN_PATH=./credentials/token.json
# GMAIL_IMPERSONATE=shared-mailbox@ulusofona.pt  # For service accounts with domain-wide delegation

# Email Content
CSV_PATH=files/<file-with-recipients>.csv
RESULTS_CSV_PATH=files/<file-with-credentials>.csv
TO_FIELD=user_email
SUBJECT=Acesso à plataforma modelos.ai.ulusofona.pt - Equipa {teams}
BODY_FILE=./templates/welcome_with_credentials.txt
# BODY=Hello {user_email}, welcome to team {teams}!  # Use this instead of BODY_FILE for inline body

# Email Headers
SENDER=<email-user>@<domain>.pt
# CC=admin@ulusofona.pt
# BCC=logs@ulusofona.pt
# REPLY_TO=support@ulusofona.pt

# Sending Options
DRY_RUN=true
DEDUPE=true
SLEEP_BETWEEN_SENDS=0.5
LIMIT=
LOG_PATH=logs/bulk_email_log.csv

# Set DRY_RUN=false when ready to send for real
